<%- include("../includes/header.ejs", {title:"Resume - View All"}); %>

<h1>Resumes</h1>

<p>Here you can view the complete list of resumes in the database, as well as the user who created them. Click a resume
    to see more information on it. If you wish to add your own resume, simply click the "Add New Resume" button.</p>
<p>_______________________________________________________</p>

<a href="/resume/add">Add New Resume</a> <br />
        <br />
        <table border="1">
            <tr><th>Resume Name</th><th>Account Name</th></tr>
            <% for(var i=0; result.length > i; i++ ) { %>
            <tr>
                <td><a href="/resume/?resume_id=<%= result[i].resume_id %>"><%= result[i].resume_name %></a></td>
                <td><a href="/account/?account_id=<%= result[i].account_id %>"><%= result[i].fname %> <%= result[i].lname %></a></td>
            </tr>
            <% } %>
        </table>

<%- include("../includes/footer.ejs"); %>